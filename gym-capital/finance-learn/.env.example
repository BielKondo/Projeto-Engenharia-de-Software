# Banco SQLite local — o arquivo será criado automaticamente em prisma/dev.db
DATABASE_URL="file:./dev.db"

# Chave secreta para assinar JWTs de sessão (mínimo 32 caracteres)
# Em produção, gere com: openssl rand -base64 32
JWT_SECRET="troque-isto-por-uma-chave-secreta-de-pelo-menos-32-caracteres"
